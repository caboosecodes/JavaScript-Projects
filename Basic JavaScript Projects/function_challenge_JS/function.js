function multiply(x, y) {
    return x * y;
}

document.getElementById("demo").innerHTML = document.write(multiply(12, 3)); 
 // in order for me to get the above code to run WITHOUT placing the script element -
 //after the element with the ID is by writing "document.write()" method before the function .